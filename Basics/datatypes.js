"use strict"; // treats all js code as newer version
// alert(3+3) // this is not work here bcoz we are using node js not browser it works in browser bcoz we know engine of js are hidden in the browser
console.log(3
    
    +3); // this is not a good practice (code readability must be high)
let name = ("Chirag") // string
let age  = 23 // number
let isLogedin = false // boolean (true/false)

// various types of datatypes :-
// number 
// BigInt (for large values)
// String ("")
// Boolean (true/false)
// null (standalone value)
// undefined (declare the variable but not assigned any value)
// symbol (unique)
// object 

console.log(typeof(isLogedin));
console.log(typeof("chirag"));
console.log(typeof null);
console.log(typeof umdefined);